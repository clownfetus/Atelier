### critical
request AES key on initial start like Pak directory with a shallow validity check (confirm AES format/length), then purge from git history

### regular
right click asset (sidebar or browser) to show menu options: Open in Explorer, Find in Atelier, Replace with Image (Only for textures), Delete edits
on initial import of asset, should start toggled on in sidebar
confirm message before export files override files with same name
fix the duplicate toast from watchdog on file edit
sidebar: path under material/vfx items dont show anything, its just '/', show path truncated
hovering over item in sidebar should show tooltip with full path
lazy load thumbnails, aka dont load thumbnails that are out of view, and keep a certain number of them in memory
forgiving rivals folder selector (selecting any subfolder along the chain from MarvelRivals/ to MarvelRivals/.../Paks/~mods should work)
use webview max compatibility gui method, ex leave undefined if better compat
server kill on webview close logic doesnt work when restarting after initial config popup saved
Ctrl+F to focus search bar and highlight everything
Update checker

### partially formed ideas
USMAP bindings auto-download with Browse option in initial setup popup too with redesign for alt sources
better top bar format (still coming up with one that includes settings and a help button that leads to https://github.com/clownfetus/Atelier#usage which should be more descriptive and hand-holdy and contain a YT shorts link)
allow multiple copies of texture files in sidebar, export only allows one of each texture to be exported at a time (still developing this idea, in the form of a completely optional advanced mode with mod profiles)
im pretty sure theres more that im forgetting rn